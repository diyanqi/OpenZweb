# Protocol engine (zju-connect)

Place the `zju-connect` binary here, or run:

```bash
../Scripts/download-core.sh
```

Expected filename: `zju-connect` (executable).

Recent GitHub release asset names look like:

- `zju-connect-darwin-arm64.zip`
- `zju-connect-darwin-amd64.zip`
